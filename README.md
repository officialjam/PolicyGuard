# PolicyGuard
PolicyScan is an AWS Lambda–based security tool that periodically scans IAM policies and role trust relationships, detects risky access patterns, and generates actionable remediation recommendations as versioned S3 reports.
